#!/bin/bash
set -e #-x

APPS=${APPS:-/mnt/apps}

SCRIPT_HOME="$( cd "$( dirname "$0" )" && pwd )"
. $SCRIPT_HOME/env.sh

docker run \
    -it -d \
    -v /vagrant/test:/vagrant \
    --name sandbox \
    --link kafka:kafka \
    $DOCKER_REPO/sandbox
    #--link redis:redis \
    #--link zookeeper:zookeeper \
    #--link kafka:kafka \
    #--link mysql:mysql \